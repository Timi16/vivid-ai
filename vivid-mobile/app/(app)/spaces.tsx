import { Screen } from "@/components/layout/screen";
import { SpacesView } from "@/features/spaces";

export default function SpacesRoute() {
  return (
    <Screen>
      <SpacesView />
    </Screen>
  );
}
