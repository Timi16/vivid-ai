[pytest]
testpaths = tests
asyncio_mode = auto
# Without the plugin, asyncio_mode is an unknown option and async tests skip
# silently while the suite still reports green. Fail collection instead.
required_plugins = pytest-asyncio
